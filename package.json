{
  "name": "palco",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Palco — visualizador de cifras com rolagem automática, transpor, capotraste, diagramas de acorde, afinador e favoritos. App offline em arquivo único.",
  "scripts": {
    "icons": "node generate-icons.mjs",
    "dev": "node build.mjs --serve",
    "watch": "node build.mjs --watch",
    "build": "node build.mjs"
  },
  "dependencies": {
    "lucide-react": "^0.383.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
